# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PlayLog' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PlayLog
pod 'OpalImagePicker'

  target 'PlayLogTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PlayLogUITests' do
    inherit! :search_paths
    # Pods for testing
pod 'OpalImagePicker'
  end

end
